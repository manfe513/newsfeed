spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql=true:
logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE
sources:
  rbk:
    actualizer-enabled: false
    page-size: 3

scheduler:
  enabled: false